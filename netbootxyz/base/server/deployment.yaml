apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
spec:
  selector:
  template:
    spec:
      containers:
      - name: server
        image: server
        env:
        - name: TFTPD_OPTS
          value: "--port-range 64001:64002"
        volumeMounts:
        - name: config
          mountPath: /config
        - name: assets
          mountPath: /assets
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - name: tftp
          containerPort: 69
          protocol: UDP
        - name: start
          containerPort: 64001
          protocol: UDP
        - name: end
          containerPort: 64002
          protocol: UDP
        - name: assets
          containerPort: 80
        - name: web
          containerPort: 3000
      volumes:
      - name: config
        persistentVolumeClaim:
          claimName: config
      - name: assets
        persistentVolumeClaim:
          claimName: assets
